# 독도 웹사이트 디자인 검토 보고서

검토일: 2026-09-25 · 브랜치: `hojun` · 대상: 홈, 독도 소개, 역사, 자료실, 방문 안내, FAQ (데스크톱 1280/1440px, 모바일 375px)

## 1. 한 줄 평가

**구조와 정보 설계는 안정적이고, 시각적 정체성은 히어로 한 곳에만 있습니다.** 시간대별로 바뀌는 히어로 사진은 이 사이트만의 아이디어입니다. 그 아래 본문은 "같은 카드, 같은 그림자, 같은 등장 애니메이션"으로 반복되어 어느 정보 사이트에서나 볼 수 있는 모습입니다. 접근성과 반응형은 기본은 갖췄으나, 아래 "높음" 항목 3개는 배포 전에 고치는 것을 권합니다.

| 영역 | 평가 | 근거 |
|---|---|---|
| 정보 구조 / 내비게이션 | 양호 | 5개 메뉴, 페이지 내 SubNav(스티키 알약)로 긴 페이지 이동이 쉬움 |
| 색상 | 양호 | 바다·하늘 계열 네이비/오션 블루 + 태극 붉은색을 "주장 라벨"에만 절제해서 사용 |
| 타이포그래피 | 양호 | 나눔명조(제목) + 노토 산스(본문)의 역할 분리가 명확. 글꼴은 woff2 서브셋으로 분할되어 있음 |
| 시각적 개성 | 보통 | 히어로 외 영역은 범용 카드 패턴 |
| 모션 | 개선 필요 | 거의 모든 블록에 같은 fade-up. `prefers-reduced-motion` 대응은 지도 컴포넌트에만 있음 |
| 접근성 | 개선 필요 | 아래 3.2 참고 |
| 성능(자산) | 개선 필요 | 로고 PNG 2.1MB, 미사용 PNG 약 12MB |

## 2. 잘된 점 (유지 권장)

1. **시간대별 히어로 크로스페이드** (`components/Hero.js`). 새벽/아침/낮/저녁/밤 5장을 현재 시각에 맞춰 보여주고, 다음 사진은 미리 받아둡니다. 주제(동해의 해 뜨는 섬)에서 나온 아이디어이고 이 사이트를 기억하게 만드는 요소입니다.
2. **색의 의미 분리**. 붉은색은 일본 측 주장(`reason-card__label`, FAQ의 A 라벨)에만, 파란색은 우리 측 근거·링크에 쓰입니다. 색이 정보를 전달합니다.
3. **거리 비교(87.4km vs 157.5km)** 를 막대로 시각화한 부분. 사이트의 핵심 주장을 한눈에 보여주는 가장 효과적인 화면입니다.
4. **본문 폭 제어**. `.section__lead`는 720px, 연표 설명은 640px로 제한되어 한 줄이 길어지지 않습니다.
5. **모바일 레이아웃**. 375px에서 가로 스크롤이 없고(문서 폭 375 = 뷰포트 375), 히어로/소개/서브내비 모두 무너지지 않습니다.
6. **대비**. 본문 보조색 `#5a6b78`은 배경 `#eeeded`에서 약 4.7:1, 연한 하늘색 섹션에서 약 4.9:1로 AA(4.5:1)를 통과합니다. 링크 파랑 `#1565a0`도 약 5.5:1입니다.
7. **커서/링크 처리**. 전역 `cursor: pointer`가 `:where()`로 명시도 0이라 개별 지정과 충돌하지 않습니다. 외부 링크에는 `rel="noopener noreferrer"`가 일관되게 붙어 있습니다.

## 3. 문제점

### 3.1 시각 디자인

**[높음] 낮 시간대 히어로 글자 가독성** — `styles/hero.css:38-50, 77-95`
- 저는 히어로 배경을 낮/아침 사진으로 바꿔서 확인했습니다. 밤 사진에서는 문제가 없지만, 낮 사진에서는 제목 "독도"가 섬 능선과 밝은 수평선에 걸치고, 부제와 좌표(`opacity .7`, `font-weight 300`)가 바위와 밝은 바다 위에 놓여 흐려집니다. 눈썹 문구는 옅은 하늘 위에 있습니다.
- 스크림(어둡게 깔아주는 그라디언트)이 위·아래 가장자리에만 있어 글자가 놓인 중앙은 사진 밝기에 그대로 노출됩니다.
- 방문자 대부분이 낮에 접속하므로, 가장 눈에 띄는 영역에서 가장 자주 발생하는 문제입니다.
- 제안: 중앙에 은은한 방사형 스크림 추가, 부제·좌표 `font-weight` 400 이상 + `opacity` 제거(그림자로 대체), 시간대별 사진마다 스크림 강도를 다르게 지정.

**[중간] 히어로 제목이 가운데에서 약 10px 왼쪽으로 치우침** — `styles/hero.css:63`
- `letter-spacing: 20px`는 마지막 글자 뒤에도 20px 여백을 만들어 글자 덩어리가 왼쪽으로 10px 밀립니다. 375px 화면에서 "독도"의 중심이 약 178px로, 화면 중심 187px보다 왼쪽입니다.
- 제안: `.hero__title`에 `padding-left: 20px`(= letter-spacing) 또는 `margin-right: -20px`. 모바일에서는 `letter-spacing`도 `clamp()`로 줄이는 편이 좋습니다.

**[중간] 카드가 모든 곳에서 동일함** — `nature.css:18`, `about.css:21`, `location.css:8`, `facts.css:2,58`, `reason.css:7`, `faq.css:7`
- 통계, 거리, 참고자료, 용어, 근거, FAQ, 표가 모두 같은 `--radius: 18px`, 같은 `--shadow`, 같은 1px 테두리를 씁니다. 정보의 중요도와 관계없이 시각적 무게가 같아서 "어디부터 봐야 하는지"가 사라집니다.
- 제안: 카드는 "클릭 가능한 것(참고자료 링크)"에만 남기고, 사실 나열(팩트 표, 용어)은 테두리·그림자 없이 구분선만 사용. 거리 비교처럼 핵심인 것만 크게.

**[중간] 모든 섹션 위에 영문 대문자 눈썹 라벨** — 18곳 (`sections.css:8`, `letter-spacing: 4px`)
- OVERVIEW, LOCATION, FACTS, HISTORY, REFERENCES, VISIT, HOW TO VISIT, FAQ 등이 한국어 제목 위에 반복됩니다. 제목이 이미 말해주는 내용을 영어로 되풀이하고 있어 정보가 늘지 않습니다.
- 제안: 삭제하거나, 남긴다면 정보가 있는 문구(예: "울릉도에서 87.4km", "기원후 512년~")로 교체.

**[중간] 스크롤 등장 애니메이션이 모든 블록에 적용됨** — `RevealInit.js` 13개 선택자, `reveal.css`
- 제목, 리드, 카드, 연표 항목, FAQ 항목 등 거의 모두가 같은 40px fade-up입니다. 모든 것이 움직이면 어느 것도 강조되지 않습니다. 연표 17개 항목이 스크롤마다 하나씩 올라오는 것도 읽는 흐름을 끊습니다.
- 제안: 히어로의 진입 연출과 거리 막대 채워지는 연출만 남기고 나머지는 제거. 최소한 `.timeline__item`, `.fact-row`, `.faq-item`, `.section__lead`는 제외.

**[낮음] 히어로 CTA 문구 "독도 알아보기 →"** — `Hero.js:104`
- 화살표 앞에 공백 두 칸이 있고, 홈 하단 CTA 3개는 화살표 없이 같은 스타일이라 서로 일관성이 없습니다. 하단 CTA 3개가 동등한 알약 버튼이라 "무엇을 먼저 누를지"도 불분명합니다.
- 제안: 주 행동 1개(근거 자료)만 채워진 버튼, 나머지는 텍스트 링크.

**[낮음] 역사 연표가 화면 절반만 사용** — `timeline.css:64`
- 데스크톱에서 설명 최대 폭이 640px이고 컨테이너는 1080px이라 오른쪽 약 40%가 비어 있습니다. 17개 항목이 세로로 길게 이어져 페이지가 깁니다.
- 제안: 연도를 왼쪽 열(고정 폭)로 빼고 내용을 오른쪽에 두는 2열 구성. 연표의 성격(연도 = 인덱스)에도 맞고, 길이도 줄어듭니다.

**[낮음] 연표에서 링크 없는 항목이 링크 있는 항목과 구분되지 않음**
- 안내문에 "제목을 누르면 관련 자료로 이동합니다"라고 있지만, 원록각서·독도 폭격 사건은 링크가 없습니다. ↗ 아이콘 유무만 다르므로 눌러도 반응이 없다고 느낄 수 있습니다. 출처 링크를 추가하거나, 안내문을 "↗ 표시가 있는 제목"으로 바꾸세요.

### 3.2 접근성 · 반응형

**[높음] 모션 감소 설정 미대응**
- `prefers-reduced-motion`은 `location-map.css:98` 한 곳에만 있습니다. 히어로 3.5초 크로스페이드, fadeUp, `.reveal`, 숫자 카운트업, 막대 채우기, `scroll-behavior: smooth`는 모두 그대로 동작합니다.
- 제안: 전역 미디어 쿼리 하나로 `animation`/`transition`을 사실상 0으로, `.reveal`은 처음부터 보이게, `html`의 smooth 스크롤 해제, 카운터는 최종값을 바로 표시.

**[높음] 모바일에서 로고가 페이지 눈썹 라벨과 겹침** — `Nav.js:28`, `nav.css`
- 로고 이미지 높이가 90px인데 내비게이션 바 높이는 70px입니다. 히어로가 없는 페이지(방문 안내, FAQ 등)를 375px에서 맨 위에서 보면, 로고 하단(약 76px)이 첫 섹션의 눈썹 라벨("VISIT", 약 70~93px)과 겹칩니다. 캡처에서도 로고 붓글씨가 "VISIT"에 닿아 있습니다.
- 제안: 로고 높이를 바(70px) 안쪽으로(예: 48~56px) 줄이고, 첫 섹션 상단 여백을 내비 높이만큼 확보.

**[중간] 메뉴 버튼 접근성/터치 영역** — `Nav.js:47-54`, `nav.css:79`
- 버튼 크기가 23×34px로 권장 최소(44×44px)보다 작습니다. `aria-label`이 "메뉴 열기"로 고정이고 `aria-expanded`가 없어 열림/닫힘 상태를 스크린리더가 알 수 없습니다. 메뉴는 화면 밖(`translateX(110%)`)으로 밀어둔 상태라 닫혀 있을 때도 키보드 Tab이 보이지 않는 링크로 들어갑니다.
- 제안: `aria-expanded`, `aria-controls` 추가, 버튼 패딩으로 44px 확보, 닫힌 상태에서 `visibility: hidden`(또는 `inert`).

**[중간] 키보드 포커스 표시 미지정**
- `:focus-visible` 스타일이 지도 컴포넌트 외에는 없어 브라우저 기본 링에 의존합니다. 어두운 반투명 내비 위나 사진 위 CTA에서 기본 링은 잘 안 보일 수 있습니다. 이번 검토에서 실제 키보드 순회는 해보지 않았으니, 배포 전에 Tab 키로 한 번 확인해 주세요.
- 제안: `:focus-visible { outline: 3px solid var(--sky); outline-offset: 3px; }`를 전역으로 두고 어두운 배경에서는 흰색으로.

**[중간] 본문 건너뛰기 링크 없음**
- 모든 페이지에서 키보드 사용자가 5개 메뉴를 매번 지나야 합니다. 레이아웃에 "본문으로 건너뛰기" 링크 하나를 추가하세요(`<main id="main">`).

**[낮음] 작은 글자**
- `nature-card__sci` 0.78rem(약 12.5px), `ref-card__url` 0.8rem, `reason-card__label` 0.78rem. 한글은 작은 크기에서 획이 뭉개지기 쉬우니 0.85rem 이상을 권장합니다.

**[낮음] 내부 링크가 `<a href>` (전체 새로고침)** — `Nav.js:27-45`, `Hero.js:103`, `app/page.js:144-150`
- Next.js의 `<Link>`를 쓰면 이동이 빠르고 미리 불러오기가 됩니다. 현재는 이동할 때마다 문서를 새로 받고 히어로 시작 연출도 매번 다시 재생됩니다. 디자인 문제는 아니지만 체감 속도에 영향이 큽니다.

**[낮음] 첫 로드 시 본문이 잠깐 안 보일 수 있음** — `RevealInit.js`
- `.reveal`(opacity 0)은 하이드레이션 이후에 JS가 붙이므로, 개발 서버에서 페이지 이동 직후 첫 섹션 제목·본문이 한동안 거의 투명한 채로 캡처되는 경우가 있었습니다(이후 정상 표시 확인). 개발 모드의 지연일 가능성이 높아 원인을 단정하지는 않습니다. 프로덕션 빌드(`npm run build && npm start`)에서 한 번 확인하고, 필요하면 첫 화면(뷰포트 안) 요소는 `.reveal` 대상에서 제외하세요.

### 3.3 코드 품질 (디자인에 영향)

- `styles/location.css:52` — `margin: 0 20 20 0;` 는 단위가 없어 **잘못된 CSS**라 무시됩니다. 같은 규칙의 `padding-left: 20px`은 가운데 정렬 인용문을 오른쪽으로 10px 밀고, `font-size: 20px`이 고정이라 사용자의 글꼴 크기 설정을 무시합니다.
- `styles/about.css:50-54` — 480px 미디어 쿼리가 기본값과 같은 `1fr 1fr`이라 아무 효과가 없습니다.
- `hero.css:59` — 눈썹 문구를 `position: relative; top: -40px`로 올려놓아 다른 요소와의 간격이 임의값에 의존합니다.
- `.hero__cta`가 히어로 외 영역(홈 하단 배너)에서도 재사용되어 이름과 역할이 어긋납니다. 이름을 `.btn-pill` 등 범용으로 바꾸는 것을 권장합니다.

### 3.4 자산 · 성능

| 항목 | 크기 | 비고 |
|---|---|---|
| `public/img/logo.png` | 2.1MB | 표시 크기는 135×90(푸터 152×101). `next/image`가 줄여서 내보내지만 원본을 SVG 또는 100KB 이하로 줄이는 게 좋음 |
| `public/img/dokdo-time/*.png` (5장) | 합계 약 12MB | 코드는 `.webp`(50~135KB)를 사용. 홈 OG 이미지만 `dokdo-morning.png`(3.1MB)를 씀 |
| `public/img/about` | 6.5MB | 소개 페이지 이미지. 실제 최적화 여부는 이번에 측정하지 않음 |

- 제안: 사용하지 않는 PNG 4장 삭제, OG 이미지는 1200×630 JPG 한 장으로 별도 제작(SNS 미리보기 로딩과 크롤러 제한 회피).

## 4. 디자인 방향 제안 (정체성)

지금 사이트는 "정리는 잘 되어 있지만 다른 정보 사이트와 바꿔 끼워도 티가 나지 않는" 상태입니다. 전면 개편보다 **히어로에서 시작한 개성을 본문으로 한 겹 이어주는 것**을 권합니다.

1. **시간대 테마를 사이트 전체로 확장**. 히어로가 밤이면 상단 내비와 마무리 배너도 밤 색조(짙은 남색)로, 낮이면 밝은 바다색으로 조금씩 달라지게 합니다. 구현은 `Hero.js`의 `getPeriod()` 결과를 `<html data-period="night">` 같은 속성으로 내보내고 CSS 변수(`--ocean`, `--closing-from` 등)만 바꾸면 됩니다. 이 사이트만 가질 수 있는 특징이 됩니다.
2. **거리 비교를 홈의 두 번째 핵심 장면으로**. 지금 두 개의 같은 카드 안에 들어 있는데, 두 섬을 한 화면에서 실제 비율로 잇는 선(87.4 vs 157.5)으로 그리면 카드 없이도 설득력이 커집니다. `LocationCompareMap`이 이미 소개 페이지에 있으니 홈에서는 단순화한 형태를 재사용할 수 있습니다.
3. **카드 위계 만들기**. 강조 1종(거리·핵심 통계), 일반 1종(링크 카드), 무(無) 1종(표·용어). 그림자와 반경을 셋으로 나눕니다.
4. **움직임은 두 곳만**. 히어로 진입, 거리 막대. 나머지는 정적.
5. **연표를 2열 인덱스형으로**. 3.1의 "역사 연표가 화면 절반만 사용" 항목 참고.

(참고: 다크 모드는 커밋 `91d6161`에서 의도적으로 제거된 것으로 보여 이번 검토 대상에서 제외했습니다. 되살릴 계획이 없다면 위 1번의 시간대 테마가 대체재가 될 수 있습니다.)

## 5. 우선순위 로드맵

| 순서 | 작업 | 예상 시간 | 파일 |
|---|---|---|---|
| 1 | 낮 시간대 히어로 스크림/글자 두께 조정 | 30분 | `hero.css` |
| 2 | 모바일 로고 높이·상단 여백(겹침 해결) | 20분 | `Nav.js`, `nav.css`, `sections.css` |
| 3 | `prefers-reduced-motion` 전역 대응 | 20분 | `base.css` 또는 `reveal.css`, `hero.css`, `CountersInit.js` |
| 4 | 히어로 제목 중앙 보정, `location.css` 잘못된 규칙 수정 | 15분 | `hero.css`, `location.css` |
| 5 | 메뉴 버튼 aria/터치 영역, `:focus-visible`, 건너뛰기 링크 | 40분 | `Nav.js`, `nav.css`, `base.css`, `layout.js` |
| 6 | 미사용 PNG 삭제, 로고·OG 이미지 경량화 | 30분 | `public/img` |
| 7 | 눈썹 라벨 정리, `.reveal` 대상 축소 | 30분 | 각 `page.js`, `RevealInit.js` |
| 8 | 연표 2열 개편, 카드 위계 정리 | 반나절 | `timeline.css`, 여러 CSS |
| 9 | 시간대 테마 확장 | 반나절 | `Hero.js`, `base.css` |

1~5번은 각각 독립적이고 위험이 낮아 한 번에 묶어 진행해도 됩니다. 8~9번은 디자인 방향을 정한 뒤 진행하는 것이 좋습니다.

## 6. 검토하지 못한 것

- 키보드만으로 전체 순회(포커스 링 실제 확인), 스크린리더 읽기 순서.
- 프로덕션 빌드에서의 로딩 속도와 Lighthouse 점수(개발 서버에서만 확인).
- 소개 페이지 갤러리 이미지의 최적화 상태, 라이트박스 동작.
- 태블릿(768px) 구간의 레이아웃.
- 방문 안내·자료실 본문 내용의 사실 정확성(디자인 검토 범위 밖).
